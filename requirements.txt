fastapi==0.115.6
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
python-multipart==0.0.17
jinja2==3.1.4
aiofiles==24.1.0
httpx==0.28.1
pydantic-settings==2.6.1
itsdangerous>=2.2.0
Pillow>=10.0.0
openpyxl>=3.1.0
xlrd>=2.0.1
pandas>=2.0.0
qrcode[pil]>=7.4.2
# PDF 渲染（病历打印里嵌入检查报告 PDF 页面图像；纯 wheel，无系统依赖）
PyMuPDF>=1.24.0
# HTML→PDF 渲染（协议签署 / 显微镜报告等出 PDF）；服务器需 apt install libpango-1.0-0 libpangoft2-1.0-0 libcairo2
weasyprint>=60.0
apscheduler>=3.10.0
# 腾讯云短信（仅当配置 TENCENT_SMS_SECRET_ID 等时才真正调用；缺包也不阻断启动）
tencentcloud-sdk-python-sms>=3.0.1100

# 可选：启用 OPENAI_API_KEY 后用于图片/视频帧的流浪猫辅助识别
openai>=1.55.0
opencv-python-headless>=4.8.0

# 企微回调消息加解密（AES-256-CBC）
pycryptodome>=3.20.0
