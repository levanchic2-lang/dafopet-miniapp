python3 << 'PYEOF'
import pathlib
R = '/srv/tnr-app/releases/current'

pathlib.Path(R+'/templates/admin_users.html').write_text('{% extends "base.html" %}\n{% block body %}\n<div class="card">\n  <h1>账号管理</h1>\n  <p class="sub">管理后台登录账号。仅超级管理员可访问此页面。</p>\n\n  {% if msg %}<div class="msg ok">{{ msg }}</div>{% endif %}\n  {% if err %}<div class="msg err">{{ err }}</div>{% endif %}\n\n  <!-- 账号列表 -->\n  <table style="width:100%;border-collapse:collapse;font-size:0.9rem;margin-top:1rem;">\n    <thead>\n      <tr style="border-bottom:2px solid var(--border);">\n        <th style="text-align:left;padding:0.5rem 0.75rem;font-weight:600;">用户名</th>\n        <th style="text-align:left;padding:0.5rem 0.75rem;font-weight:600;">角色</th>\n        <th style="text-align:left;padding:0.5rem 0.75rem;font-weight:600;">状态</th>\n        <th style="text-align:left;padding:0.5rem 0.75rem;font-weight:600;">创建时间</th>\n        <th style="padding:0.5rem 0.75rem;"></th>\n      </tr>\n    </thead>\n    <tbody>\n      {% for u in users %}\n      <tr style="border-bottom:1px solid var(--border);{% if not u.is_active %}opacity:0.55;{% endif %}">\n        <td style="padding:0.55rem 0.75rem;">\n          {{ u.username }}\n          {% if u.username == current_username %}<span style="font-size:0.75rem;color:var(--muted);margin-left:6px;">（当前）</span>{% endif %}\n        </td>\n        <td style="padding:0.55rem 0.75rem;">\n          {% if u.role == \'superadmin\' %}\n            <span class="badge b-done">超级管理员</span>\n          {% else %}\n            <span class="badge b-pending">员工</span>\n          {% endif %}\n        </td>\n        <td style="padding:0.55rem 0.75rem;">\n          {% if u.is_active %}\n            <span class="badge b-ok">启用</span>\n          {% else %}\n            <span class="badge b-no">已停用</span>\n          {% endif %}\n        </td>\n        <td style="padding:0.55rem 0.75rem;color:var(--muted);">{{ u.created_at.strftime(\'%Y-%m-%d\') }}</td>\n        <td style="padding:0.55rem 0.75rem;text-align:right;">\n          <div style="display:flex;gap:0.5rem;justify-content:flex-end;flex-wrap:wrap;">\n            <!-- 停用 / 启用 -->\n            {% if u.username != current_username %}\n            <form method="post" action="/admin/users/{{ u.id }}/toggle" style="margin:0;">\n              <input type="hidden" name="csrf_token" value="{{ csrf_token }}" />\n              <button type="submit" class="secondary" style="font-size:0.8rem;padding:0.3rem 0.7rem;">\n                {{ \'停用\' if u.is_active else \'启用\' }}\n              </button>\n            </form>\n            {% endif %}\n            <!-- 重置密码 -->\n            <button type="button" class="secondary" style="font-size:0.8rem;padding:0.3rem 0.7rem;"\n              onclick="toggleReset({{ u.id }})">重置密码</button>\n          </div>\n          <!-- 重置密码表单（折叠） -->\n          <div id="reset-{{ u.id }}" style="display:none;margin-top:0.5rem;">\n            <form method="post" action="/admin/users/{{ u.id }}/reset-password"\n                  style="display:flex;gap:0.4rem;align-items:center;flex-wrap:wrap;">\n              <input type="hidden" name="csrf_token" value="{{ csrf_token }}" />\n              <input type="password" name="new_password" placeholder="新密码（至少6位）"\n                     required minlength="6"\n                     style="width:160px;font-size:0.85rem;padding:0.3rem 0.55rem;" />\n              <button type="submit" style="font-size:0.8rem;padding:0.3rem 0.7rem;">确认重置</button>\n              <button type="button" class="secondary" style="font-size:0.8rem;padding:0.3rem 0.7rem;"\n                onclick="toggleReset({{ u.id }})">取消</button>\n            </form>\n          </div>\n        </td>\n      </tr>\n      {% else %}\n      <tr>\n        <td colspan="5" style="padding:1rem 0.75rem;color:var(--muted);text-align:center;">\n          暂无账号，请在下方创建第一个账号。\n        </td>\n      </tr>\n      {% endfor %}\n    </tbody>\n  </table>\n</div>\n\n<!-- 新建账号 -->\n<div class="card">\n  <h1 style="font-size:1.1rem;">新建账号</h1>\n  <form method="post" action="/admin/users/create">\n    <input type="hidden" name="csrf_token" value="{{ csrf_token }}" />\n    <div class="row2">\n      <div>\n        <label for="new_username">用户名</label>\n        <input type="text" id="new_username" name="username" required autocomplete="off"\n               placeholder="字母、数字、下划线" />\n      </div>\n      <div>\n        <label for="new_password">初始密码（至少6位）</label>\n        <input type="password" id="new_password" name="password" required minlength="6"\n               autocomplete="new-password" />\n      </div>\n    </div>\n    <label for="new_role">角色</label>\n    <select id="new_role" name="role">\n      <option value="staff" selected>员工（可查看 + 改状态）</option>\n      <option value="superadmin">超级管理员（全权限）</option>\n    </select>\n    <button type="submit" style="margin-top:1rem;">创建账号</button>\n  </form>\n</div>\n\n<div style="margin-top:0.5rem;">\n  <a href="/admin" class="btn secondary" style="font-size:0.88rem;">返回医院后台</a>\n</div>\n\n{% block scripts %}\n<script>\nfunction toggleReset(id) {\n  var el = document.getElementById(\'reset-\' + id);\n  el.style.display = el.style.display === \'none\' ? \'block\' : \'none\';\n}\n</script>\n{% endblock %}\n{% endblock %}\n', encoding='utf-8')
print('updated: templates/admin_users.html')

print('Part 2 done')
PYEOF